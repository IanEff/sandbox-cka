# Missing Endpoints

A service `backend-svc` in namespace `endpoint-ns` reports no endpoints. Applications depending on this service are failing.

The deployment `backend` exists and pods are running.

Find and fix the issue so the service has working endpoints.
