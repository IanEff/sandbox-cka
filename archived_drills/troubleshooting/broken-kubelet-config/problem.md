# Broken Kubelet Configuration

One of the worker nodes is reporting `NotReady`.

**Task:**

1. Identify the broken node.
2. Diagnose the issue with the kubelet service.
3. Fix the configuration so the node becomes `Ready`.

**Hint:** Check the kubelet logs and configuration files.
