# Drill: The Crashing Pod

A pod named `web-frontend` in the `dev` namespace is in a CrashLoopBackOff state.

**Your Task:**
1. Diagnose why the pod is failing.
2. Fix the issue so the pod enters the `Running` state.

*Hint: Check the logs and the command being executed.*
