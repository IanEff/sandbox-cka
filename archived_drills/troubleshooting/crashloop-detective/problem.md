# CrashLoopBackOff

A pod named `webapp` in namespace `crashloop-ns` is in CrashLoopBackOff.

Investigate and fix the pod so it runs successfully.

**Constraints:**
- The image must remain `nginx:alpine`.
- The pod name must remain `webapp`.
