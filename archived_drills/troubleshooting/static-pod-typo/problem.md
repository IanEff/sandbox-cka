# Static Pod Typo

A static pod named `control-monitor` is supposed to be running on the control plane node `ubukubu-control`.
However, it is not appearing in the pod list.

**Task:**
Find the static pod manifest and fix the issue so the pod starts running.
