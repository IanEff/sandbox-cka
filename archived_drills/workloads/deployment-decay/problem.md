# Drill: Deployment Decay

The deployment `web-frontend` in namespace `lifecycle` has been deployed but the pods are not becoming Ready.

The developers insist the application listens on port 8080.

**Your Task:**
Identify the issue preventing the pods from becoming Ready and fix it.
Ensure all replicas are Available and Ready.
