# Manual Scheduling

Create a pod named `manual-pod` using image `nginx:alpine`.
The pod must be scheduled on node `ubukubu-node-1`.

**Constraints:**
- Do not use `nodeSelector`.
- Do not use Affinity or Anti-Affinity.
- The pod must be running.
