# Etcd Data Directory

Identify the directory path where `etcd` stores its data on the controlplane node.
Write this path to the file `/home/vagrant/etcd-data.txt`.
The file should contain only the path, e.g., `/var/lib/something`.
