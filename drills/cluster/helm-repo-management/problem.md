# Drill: Helm Repo Management

## Problem

1. Add the Bitnami Helm repository: `https://charts.bitnami.com/bitnami`
2. Search for the `apache` chart.
3. Install the `apache` chart with release name `web-server` in a new namespace `web-ns`.

## Hints

- `helm repo add`
- `helm install`
