# JSONPath Node Extraction

## Problem

List all nodes in the cluster.

Extract **only** the `Name` and the `InternalIP` of each node.

The output should be sorted by `Name` in ascending order.
The output format should be space-separated: `NODE_NAME INTERNAL_IP`.

Write the result to `/opt/course/1/nodes.txt`.
