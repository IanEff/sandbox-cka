# Crash Config Troubleshooting

The Deployment `web-frontend` in namespace `drill-crash` is failing to start.
Diagnose the issue and fix it so that the Pods enter the `Running` and `Ready` state.
Do not delete the Deployment or the Service.
