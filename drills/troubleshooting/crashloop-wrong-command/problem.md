# CrashLoop Wrong Command

The pod `cmd-pod` is in a generic CrashLoopBackOff.

1. Inspect the logs/events to find the reason.
2. Fix the pod so it runs successfully.
3. The pod is expected to sleep for 3600 seconds.
