# Deployment Image Patch

The Deployment `backend-api` is running `nginx:1.18`.
Update the image to `nginx:1.19` using an imperative command (like `kubectl set image` or `kubectl patch`).
Do not edit the deployment manifest manually if possible.
