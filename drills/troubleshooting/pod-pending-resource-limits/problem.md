# Pod Pending Resource Limits

The pod `huge-pod` is getting stuck in a Pending state.

1. Investigate why the pod is Pending.
2. Fix the issue so the pod can transition to Running.
3. You may adjust the resource requests/limits, but keep them defined.
